<view class="top">
		<view class="topitem"
		 wx:for="{{topItems}}"
			wx:key="index"
			 data-Touchid="{{item}}"
				bindtap="topTab"
				>
			<image class="itemImg" src="{{item.icon}}"></image>
			<text class="title">{{item.title}}</text>
		</view>
	</view>
	